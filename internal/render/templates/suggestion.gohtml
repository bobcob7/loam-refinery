{{- /* internal/render/templates/suggestion.gohtml — one suggestion card
(§7.4): pros and cons in their own two-column sub-grid, effort/scope as
badges, an optional code excerpt at the bottom ({{template "code"}}).
Bead .9 owns this file's content. Left as an empty define here so a
future {{template "suggestion"}} call resolves without bead .9 having
to create the file first. */ -}}
{{define "suggestion"}}{{end}}
